spring:
  profile: ${EUREKA_INSTANCE}
eureka:
  instance:
    hostname: ${EUREKA_INSTANCE}
  client:
    service-url:
      defaultZone: http://${EUREKA_PEER_INSTANCE}/eureka/
    register-with-eureka: true
    fetch-registry: true
logging:
  level: debug

